# Dockerfile for Spark Worker with curl installed
FROM bitnami/spark:3.5.1

USER root

# Install curl for health checks
RUN apt-get update && \
    apt-get install -y curl && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Switch back to non-root user
USER 1001